apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: online-bookstore
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: order-service:local
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: bookstore-config
              key: ASPNETCORE_ENVIRONMENT
        - name: DOTNET_RUNNING_IN_CONTAINER
          valueFrom:
            configMapKeyRef:
              name: bookstore-config
              key: DOTNET_RUNNING_IN_CONTAINER
        - name: PAYMENT_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: bookstore-config
              key: PAYMENT_BASE_URL
        - name: Jwt__Secret
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: Jwt__Secret
        - name: Jwt__Issuer
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: Jwt__Issuer
        - name: Jwt__Audience
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: Jwt__Audience
        - name: ORDERS_SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: ORDERS_SUPABASE_URL
        - name: ORDERS_SUPABASE_KEY
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: ORDERS_SUPABASE_KEY
        - name: ORDERS_SUPABASE_SERVICE_KEY
          valueFrom:
            secretKeyRef:
              name: bookstore-secrets
              key: ORDERS_SUPABASE_SERVICE_KEY
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: online-bookstore
spec:
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
